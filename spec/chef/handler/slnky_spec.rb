require 'spec_helper'

describe Chef::Handler::Slnky do
  it 'has a version number' do
    expect(Chef::Handler::Slnky::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
